---
title: Project Memory
status: active
updated_on: 2026-07-23
tags:
  - minimalist-chat
  - memory
  - project-context
---

# Project Memory

## Product

Minimalist.chat is a room-centered communication product. Its current public positioning emphasizes calm, organized group chat with catch-up digests, focus modes, decisions, action items, scheduled messages, templates, roles, and offline-first reading. The broader app includes collaborative room features such as documents, whiteboards, events, tasks, calls, search, moderation, and AI-assisted workflows.

Sources: [[10 Product/Current/Current Product Overview|Current Product Overview]], [[20 Research/Claude Feature Research and Implementation Plan|Feature Research]].

## Technical shape

- React 19 + Vite 6 frontend.
- Firebase Auth, Realtime Database, Functions, Hosting, and Storage-oriented workflows.
- Capacitor Android/iOS packaging.
- Express server path plus Socket.IO dependency.
- Stripe billing integration.
- Node.js 22 or newer is required by `package.json`.

The repository is in an active React/Vite migration and has substantial existing local changes. Preserve unrelated work and avoid broad rewrites.

## Security and AI invariants

- The public client must never call raw Ollama directly.
- Browser AI requests go through the authenticated Firebase `aiGateway`.
- The gateway enforces identity, room access, quota/“Bananas,” abuse controls, and audit logging before calling the protected bridge.
- The bridge requires a bearer token and must remain private; fail closed when bridge identity/configuration is invalid.
- Remote Analysis uses a separate read-only Windows agent, Cloudflare hostname, and exact-owner Access policy; it never exposes desktop administration controls or falls back to controlling the client computer.
- Do not claim end-to-end encryption unless the actual implementation and threat model support that claim.

Sources: [[40 Operations/Protected Ollama Public Gateway|Protected Ollama Public Gateway]], [[40 Operations/Remote Analysis Access|Remote Analysis Access]].

## Account and room billing boundary

Account subscriptions and room billing are separate Stripe scopes. An existing account subscription opens the dedicated account Customer Portal instead of creating a duplicate Checkout session; account and room portal configurations must never allow plan changes to cross scopes. Server code reuses a stored Stripe customer only when it exists in the current mode and belongs to the authenticated Firebase UID, otherwise it idempotently replaces missing, deleted, wrong-owner, or test-mode records and clears stale account entitlements. Canonical sources: `functions/index.js`, `functions/stripe-customer.js`, `src/features/billing/billingActions.js`, `src/features/billing/AccountBillingPanel.jsx`, and `tools/account-billing.test.mjs`.

## Quality baseline

The June 2026 audit records extensive hardening across RTDB rules, Functions, mobile safe areas, AI gateway behavior, room permissions, notifications, PM inbox fan-out, calls, and test coverage. Treat it as an audit snapshot rather than proof that every claim still matches the current dirty worktree.

Recorded Lighthouse checkpoints:

- Desktop baseline: 97 / 96 / 100 / 91.
- Mobile baseline: 54 / 96 / 92 / 91.
- Desktop final live: 100 / 100 / 100 / 91.
- Mobile final live: 99 / 100 / 100 / 91.

Order: Performance / Accessibility / Best Practices / SEO. See [[30 Audits/Lighthouse History|Lighthouse History]] and [[30 Audits/Full Stack Audit Follow-up|Full Stack Audit Follow-up]].

## Product opportunities recorded in research

The imported research prioritizes quiet-hours UI, multiple reactions, Bananas visibility, poll completion, unread navigation, safe link unfurls, slash-command honesty, and voice messages. Revalidate against the current code and product direction before implementation.

## Operating guardrails

- Do not deploy, commit, push, or open a PR unless the user explicitly asks.
- Keep source documents authoritative; regenerate imported copies with `tools/import-obsidian-vault.py`.
- Prefer [[90 Memory/Source Catalog|Source Catalog]] for provenance and [[00 Home/Minimalist Chat Knowledge Hub|Knowledge Hub]] for navigation.
- Record durable decisions below; put unclassified captures in [[90 Memory/Inbox|Memory Inbox]].

## Durable decisions

- 2026-07-23 — Every Firebase Hosting publish must create a new visible build number, including a republish of identical source, while real-user performance monitoring remains grouped by the stable source release. The common Hosting predeploy hook must run the RUM gate, allocate the ID, acquire the atomic lifecycle lock, create fresh output, and verify exact bootstrap URLs, Vite identity metadata, and the compiled Settings/service-worker consumers; postdeploy must verify exact live build/version/source metadata with bounded requests before releasing the lock. Direct Firebase Hosting commands, filtered `hosting:<target>` releases, and the Stripe billing publisher use the same hooks, while owner-scoped cleanup handles failures before postdeploy. Functions-, database-, rules-, and Storage-only deployments do not replace the web application and therefore do not create a new web build number. Canonical runbook: [[40 Operations/Release Publishing|Release Publishing]]. [Source: User, build-number publishing requirement, 2026-07-23; `tools/publish-build-number.mjs`; `tools/source-release-id.mjs`; `tools/prepare-hosting-publish.mjs`; `tools/verify-hosting-publish.mjs`; `tools/deploy-firebase-hourly.ps1`; `tools/deploy-stripe-billing.ps1`; `vite.config.js`; `src/features/performance/realUserPerformance.js`; `firebase.json`; `tools/publish-build-number.test.mjs`]
- 2026-07-22 — The local room-chat modernization is implemented as eight interoperable, backward-compatible upgrades: (1) per-user/per-channel read cursors, explicit mark-unread anchors, an unread separator, and jump-to-first-unread; (2) message threads with reply counts, following, thread read state, and reply notifications; (3) a bounded, cached workspace search palette with room/channel/author/date/content filters and exact message jumps; (4) server-owned reporting/moderation plus a durable IndexedDB outbox, upload progress/cancel/preview, and constrained Storage rules; (5) announcement and role-gated channel modes enforced in both server sends and RTDB rules, plus private scheduled-message queues and due-message workers; (6) timezone-aware recurring events with RSVP/reminder state, room-call entry, and DST-correct Google Calendar export; (7) single- or multiple-choice polls with optional anonymity and server/rules-enforced closing times; and (8) native chat UI coverage for English, Spanish, Simplified Chinese, French, German, Brazilian Portuguese, Japanese, Arabic/RTL, and Hindi, with a server-side translation contract and private deterministic cache keys. Banned and muted accounts are rechecked for immediate and scheduled server sends, private moderation/scheduling payloads remain server-owned, and no client may forge sender identity. ChatCore defers the enhancement bundle to protect startup budgets. Canonical sources: `src/features/chat-core/ChatCore.jsx`, `src/features/chat-core/ChatEnhancements.jsx`, `src/features/chat-core/readState.js`, `src/features/chat-core/threadModel.js`, `src/features/chat-core/channelModel.js`, `src/features/chat-core/scheduledMessageModel.js`, `src/features/chat-core/outboxStore.js`, `src/features/chat-core/pollModel.js`, `src/features/search/Search.jsx`, `src/features/search/workspaceSearchService.js`, `src/features/events/Events.jsx`, `src/features/events/eventModel.js`, `src/features/calendar/Calendar.jsx`, `src/lib/i18n.js`, `functions/room-moderation.js`, `functions/room-scheduling.js`, `functions/message-translation-contracts.js`, `database.rules.json`, and `storage.rules`. Verification passed lint, the production build and all performance budgets, 73 focused chat/search/moderation/scheduling tests, 18 calendar/event tests, 17 i18n/translation tests, all 294 regression contracts, and the full RTDB and Storage emulator rule suites. Public/login and deterministic desktop/mobile browser checks passed with no horizontal overflow or console warnings; authenticated-only rendering was verified through component, contract, and emulator coverage because no signed-in QA account was used. The work remains local; nothing was deployed, committed, or pushed.
- 2026-07-22 — The local Help, language, room-activity, and database-safety slice is implemented as one bounded compatibility layer. The public FAQ is now a searchable, topic-filtered, responsive Help Center whose content is split into stable IDs and localized English, Spanish, and Simplified Chinese catalogs. Locale selection is detected from browser preferences, persisted locally, falls back to English, and keeps the document `lang` and `dir` attributes synchronized; dates and search normalization use locale-aware platform APIs. Settings adds a Language & Help surface with the native language selector, product-tour restart, support links, and live Realtime Database connection state. Room activity stores stable versioned event codes plus arguments while retaining legacy text fallback, so Room Home can localize activity and dates without parsing English prose. Related room membership, per-user index, and activity-log mutations use atomic multi-location updates, while RTDB rules add targeted query indexes and validation for structured logs, presence, preferences, reminders, and bounded usage data. Canonical sources: `src/lib/i18n.js`, `src/lib/useLocale.js`, `src/lib/databaseConnection.js`, `src/main.jsx`, `src/content/marketingContent.js`, `src/content/helpContent.js`, `src/pages/MarketingPages.jsx`, `src/pages/helpCenter.css`, `src/pages/ChatPage.jsx`, `src/features/settings/LanguageHelpSettings.jsx`, `src/features/settings/languageHelp.css`, `src/features/settings/settingsService.js`, `src/features/presence/presenceService.js`, `src/features/rooms/roomActivity.js`, `src/features/rooms/roomControls.js`, `src/features/room-home/RoomHome.jsx`, `database.rules.json`, `tools/i18n-help.test.mjs`, `tools/room-activity.test.mjs`, and `tools/rtdb-rules-smoke-test.mjs`. Verification passed 10 localization/activity tests, 211 RTDB rule cases, 294 broader regression checks, focused ESLint, the production build and every performance budget, plus signed-in and public desktop/mobile interaction checks with no horizontal overflow. The change remains local; nothing was deployed, committed, or pushed.
- 2026-07-22 — Room Home keeps its existing identity → stats → Room Pulse → detail hierarchy; motion is limited to a one-shot **Room signal** inside the Room Pulse module. A 2px yellow trace grows and fades while the pulse icon gives one beat, triggered only by `#room-view-home.active:not(.hidden)` so it replays on tab re-entry without React state, timers, observers, listeners, or remounting. The desktop trace/icon motions are bounded to 720/460 ms and mobile to 560/400 ms, animate only opacity and transform, never loop, do not animate the persistent scroll root or `.rh-stat` elements already owned by modern-theme reveal motion, and explicitly stop for system reduced motion plus the app's `prefers-reduced-motion` and `performance-low` modes. Canonical sources: `src/features/room-home/roomHome.css` and `tools/audit-regression-check.mjs`. Verification passed focused ESLint, all 294 regression checks, 16 lifecycle tests, the production build, CSS minification, and every performance budget. Signed-in visual/browser verification remains pending because the in-app browser blocked the localhost URL under its navigation policy; no alternate browser path was used. No deployment was performed.
- 2026-07-21 — Winston’s full local agent upgrade is implemented as an authenticated, bounded capability layer rather than an unrestricted chatbot. Room AI and Winston expose Stop, Retry, Edit & resend, real queued-local partial streaming, exact provider/model disclosure, server-validated evidence cards, safe source navigation, and typed `create_task` proposals that require confirmation, expire after 15 minutes, and write idempotently. Direct and hosted replies remain truthfully buffered; an already-running durable job cannot yet be interrupted, while an unclaimed queued job can be cancelled. Cross-room briefings revalidate membership and accept at most eight explicitly selected rooms; private memory requires affirmative consent, supports personal or room scope plus expiry, caps at 80 cards, and is server-only in RTDB. Vision accepts one JPEG/PNG/WebP image per request, prepares up to a 20 MiB source locally into an allowlisted payload no larger than 5 MiB, and never queues or persists image bytes. Local-only routing is enforced server-side and waits for the PC instead of overflowing to Cloudflare or Groq. The protected bridge is live with four weighted physical execution units, a 100-request local waiting queue, a 120-minute Auto idle window, authenticated non-generative model preload, privacy-safe telemetry, and the separate durable logical 10 local / 40 Cloudflare / 40 Groq provider contract whose overflow waits instead of disappearing. A genuine isolated 20-request promotion gate rejected `gpt-oss:20b`: Qwen3 14B scored 98.57% (69/70), GPT-OSS 20B scored 95.71% (67/70), the candidate regressed on both refusal cases, and its −2.86-point quality change missed the required +5 despite passing latency; Smart therefore remains `qwen3:14b`. Verification passed 113 AI tests, 279 regression checks, RTDB rules including raw-client denial of agent memory/actions, focused ESLint and syntax checks, production build and performance budgets, protected bridge/tunnel health, authenticated preload, and signed-in desktop plus 390×844 browser checks with no Winston horizontal overflow. The local protected bridge was restarted to activate its runtime changes; the website/Functions were not deployed, and nothing was committed or pushed. Canonical sources: `src/features/ai/AI.jsx`, `src/features/ai/AiAgentControls.jsx`, `src/features/ai/aiAgentUi.js`, `src/features/ai/localAiClient.js`, `functions/index.js`, `functions/ai-context.js`, `functions/ai-agent-contracts.js`, `functions/ai-agent-stream.js`, `database.rules.json`, `tools/ollama-bridge/ollama-bridge.cjs`, `tools/ai-model-quality-eval.mjs`, and `artifacts/ai-quality/winston-qwen3-14b-vs-gpt-oss-20b.json`.
- 2026-07-21 — Room Catch-up is a truthful, bounded recent-activity review tool rather than an unread claim: first use says **Recent activity**, a known explicit review boundary may say **new updates**, and a missing boundary is reported as a lower-bound recent batch. The deterministic model examines at most 18 loaded messages, counts contributors by UID where available, uses strict `@handle` matching, excludes the viewer’s own human messages while retaining requester-owned automation output, and prioritizes human mentions, important items, actions, questions, and files ahead of automation failures. **Review** reuses the scoped message-jump pipeline with Previous/Next/Done controls; **Open Room AI** never starts inference or spends Bananas automatically; **Save task** opens an editable confirmation before any room write; and **Mark reviewed** stores only the latest reviewed message ID per Firebase UID + room + channel on the current device, with a six-second Undo. The independent default-on visibility preference and global collapsed preference remain separate. The visual contract is a quiet white utility row: medium-weight type, inline metadata without pills, unboxed icons and text actions, hairline separation, no raised hover movement, and yellow limited to a tiny activity/focus accent. Wide desktop Catch-up is one 44px row and retracts to 38px; a narrow fine-pointer pane reflows details into a 64px compact pair, while touch tablets, phones, and short landscape use a 50px summary or 48px retracted row with a visible 44px chevron and no persistent detail row. Catch-up stays available in read-only rooms, Quick Replies remain independent, and footer changes may pin only an already-latest reader while preserving an older-history message and offset. Canonical sources: `src/features/chat-core/ChatCore.jsx`, `src/features/chat-core/roomCatchUpModel.js`, `src/features/chat-core/catchUpPreference.js`, `src/features/chat-core/roomCatchUp.css`, `tools/room-catchup-model.test.mjs`, `tools/room-catchup-preference.test.mjs`, and `tools/audit-regression-check.mjs`. Verification passed 22 chat tests, 16 lifecycle tests, all 292 regression checks, focused ESLint, the production build and performance budgets, plus signed-in 1440×900, 769×1024 fine/coarse tablet, 390×844, 320×568, and 844×390 Browser checks with no horizontal overflow or feature-related console errors. Visual source: `artifacts/design/chat-assist-footer-concept-v2.png`. No deployment was performed.
- 2026-07-21 — Quick Replies are a deterministic, contextual draft aid rather than a generic action bar. The model scans at most 12 loaded messages, targets an explicit reply first, suppresses stale suggestions after the viewer’s newest human message, applies strict `@handle` boundaries, ranks message meaning ahead of bot identity, and keeps attachment, poll, reminder, and automation-error wording honest about actions that have not occurred. Suggestions never send automatically: choosing one persists the room/channel-scoped draft, moves focus and the caret into the composer, marks typing briefly, and announces that the idea should be reviewed before sending. The rail hides during a draft, read-only state, or active send and remains independent from Room Catch-up. Its stable chevron replaces one-time dismissal, keeps keyboard focus during source-message updates, and saves a versioned collapsed marker per Firebase account on the current device; expanding removes that marker so the safe default remains open. The UI is a deferred, unboxed **Reply ideas** rail with inline source context, three content-sized neutral suggestions, only a tiny yellow dot/faint tint on the first idea, bare Refresh and disclosure controls, stable non-translating hover states, two-tone inset focus rings, and horizontal phone overflow. It is 42px open / 36px retracted on fine-pointer desktop and 50px / 48px on coarse layouts with every visible control at least 44px. Solid-yellow suggestion slabs, hard shadows, and boxed action icons are explicitly rejected by regression coverage. Footer resizing preserves an old-history reader’s exact message ID, pixel offset, and `scrollTop` while keeping an already-bottom reader pinned. Canonical sources: `src/features/chat-core/ChatCore.jsx`, `src/features/chat-core/QuickReplies.jsx`, `src/features/chat-core/quickReplyModel.js`, `src/features/chat-core/quickRepliesPreference.js`, `src/features/chat-core/quickReplies.css`, `tools/quick-replies-model.test.mjs`, `tools/quick-replies-preference.test.mjs`, and `tools/audit-regression-check.mjs`. Verification passed 22 chat tests, 16 lifecycle tests, focused ESLint, all 292 regression checks, the production build and performance budgets, signed-in insert/refresh/collapse/reload/focus interaction checks, 1440×900, 769×1024, 390×844, 320×568, and 844×390 responsive checks, zero horizontal overflow, and no feature-related Browser console errors. Visual source: `artifacts/design/chat-assist-footer-concept-v2.png`. No deployment was performed.
- 2026-07-21 — Room AI and Winston intercept explicit stock-quote intent as a deterministic tool request before waking or charging the language model. The browser sends only a normalized ticker to the existing authenticated Firebase `stockQuote` function, which retains Yahoo Finance with Stooq fallback; it never calls those providers directly. A vague request may infer the ticker only from the latest successful **Stock Price Bot** room message, while failed quote text such as `APPL` errors is ignored. Replies identify the latest available price, change, as-of time, provider, delay warning, and a credential-free source link; endpoint failures produce a truthful unavailable response and must never recycle a cached room price as current. All non-stock prompts continue through the normal authenticated AI gateway and provider queue. Canonical sources: `src/features/ai/stockQuoteTool.js`, `src/features/ai/localAiClient.js`, `src/features/ai/AI.jsx`, and `tools/ai-stock-quote-tool.test.mjs`. Verification passed six focused stock-tool tests, five rich-response tests, targeted ESLint, 78 AI tests, 271 regression checks, the production build and performance budgets, a genuine signed-in AAPL response from Yahoo Finance, and a 390×844 no-horizontal-overflow check with no console errors. No deployment was performed.
- 2026-07-21 — Room AI and Winston assistant replies share a safe rich-response renderer: common AI Markdown becomes semantic headings, paragraphs, unordered/ordered lists, quotes, rules, inline/fenced code, emphasis, and links, while user-authored prompts remain plain text. Raw reply text is escaped before inline formatting, the React conversion allowlists elements and attributes, and only credential-free `http`/`https` links may open with `noopener noreferrer`. Each reply may show at most three deduplicated URL cards derived locally from the validated hostname/path; the client must not fetch third-party metadata, images, or iframes. Full unfurls still require an SSRF-safe server fetch/cache. Canonical sources: `src/features/ai/AiResponseContent.jsx`, `src/features/ai/aiResponseFormatting.js`, `src/features/ai/aiResponseContent.css`, `src/features/ai/AI.jsx`, `src/features/ai/room-ai.css`, and `tools/ai-response-formatting.test.mjs`. Verification passed 5 focused tests, targeted ESLint, 78 AI tests, 271 regression checks, production build/performance budgets, genuine local-Ollama formatting and URL-card responses, desktop visual inspection, and a 390×844 no-horizontal-overflow check. No deployment was performed.
- 2026-07-21 — The Appearance controls labeled **Winston on desktop** and **Winston on mobile** govern only the corresponding Winston navigation trigger; Room AI and other AI-assisted tools remain independent. Disabling a surface must remove its trigger from the rendered navigation immediately and after reload using synchronized `hidden`, `aria-hidden`, preference classes, and a dock-scoped `display: none !important` rule that wins over shared mobile-button layout styles. Canonical sources: `src/features/ai/personalAgentPreference.js`, `src/features/settings/settingsService.js`, `src/features/shell/chatApp.js`, `src/features/shell/roomFeatureLoaders.js`, `public/mobile-app-dock.css`, and `tools/audit-regression-check.mjs`. Verification passed the signed-in 390×844 enable/disable/reload interaction, desktop independence check, 269 regression checks, 16 lifecycle checks, targeted ESLint, production build, and all performance budgets. No deployment was performed.
- 2026-07-21 — The signed-in mobile app navigation is a five-destination dock ordered **Rooms, Contacts, Winston, Updates, More**. Keep the original Minimalist mascot and Winston’s standard sparkle navigation glyph unchanged; place Search, Vault, and Settings in the labelled More utility sheet instead of crowding the primary bar. The dock and sheet must use the app’s existing black/white/yellow theme, full readable labels, selected-state feedback, at least 44px targets, exact safe-area clearance, and a centered 580px tablet cap through 768px; switch cleanly to desktop navigation at 769px. All destinations share synchronized active, `aria-current`, `aria-expanded`, and floating-surface state. Treat More as an overlay rather than a destination change: opening it must preserve the selected room tab and any current Contacts, Winston, or Updates surface underneath, keep the sheet visually above that surface, and close or replace the current surface only after the user chooses Search, Vault, or Settings. More, Search, Vault, Settings, Rooms, Contacts, Winston, and Updates must support bounded open/close lifecycles, Escape/outside dismissal where appropriate, and predictable focus entry/restoration. Keep the new dock stylesheet on the deferred mobile CSS path so the signed-in CSS performance budget remains below 225 KiB. Canonical sources: `src/pages/ChatPage.jsx`, `src/features/shell/chatShellControls.js`, `src/features/shell/roomFeatureLoaders.js`, `src/features/search/Search.jsx`, `public/mobile-app-dock.css`, and `tools/audit-regression-check.mjs`. Verification passed targeted ESLint, 268 regression checks, 16 lifecycle checks, 16 focused PM/audio/Winston tests, the production build and all performance budgets, the complete public desktop/mobile UI smoke suite, and signed-in browser checks at 320×568, 390×844, 768×1024, and the 768/769 breakpoint with no console issues or horizontal overflow. No deployment was performed.
- 2026-07-22 — GBrain is the guarded, private local memory for Minimalist Chat. It uses tray Ollama only on `127.0.0.1:11434` with `mxbai-embed-large` at 1,024 dimensions and balanced expansion-free search; Winston's isolated `11435` runtime is forbidden. The file configuration selects `gbrain-base-v2`, but the live database still reports `gbrain-base` and offers v2 only as a manual migration, so the isolated accepted v2 trial must not be described as a live migration. The current owned inventory has a 36-note federated vault mirror and a 743-file explicit-only authored-code mirror, each checked by per-file SHA-256 against its current source. The native MCP path is an authority proxy that verifies candidates against exact source manifests, collapses duplicate `(source_id, slug)` identities, prefers current/canonical evidence, and exposes citation-aware results with local paths, heuristic conflict reporting, and explicit weak/conflicting-evidence abstention. Six timeline images have SHA-bound local `qwen3.6:latest` vision sidecars; generated descriptions are secondary evidence, never deployment proof. A separate eight-image archived visual-history collection is represented through one source-authored context note plus hash-verified vault assets and sanitized provenance; GBrain indexes the conservative context rather than raw transcript metadata. The 100-case V3 gate deduplicates before every rank and negative metric and passed all seven production quality/source thresholds in the acceptance run; latency is reported but not gated. The latest maintenance graph has 1,265 nodes / 2,662 edges, zero isolated nodes, and 39 degree-one-or-less nodes; deterministic enrichment refuses any quality regression, while full semantic document refresh remains manual through the Graphify skill. `tools/gbrain/Invoke-GBrainMaintenance.ps1` owns backup verification, restore drill, note/code refresh, cached vision, V3 evaluation, relationship validation, and exact MCP restoration. The localhost-only health dashboard and owned Sunday 03:00 limited/interactive task must validate their full runtime contracts. Retain seven verified local snapshots, keep Jina reranking and dream/autopilot/background writers disabled, and never treat this local setup as a deployment or publication. Canonical setup and evidence: [[90 Memory/GBrain Setup|GBrain Setup]] and [[30 Audits/GBrain Upgrade and Retrieval Audit 2026-07-21|GBrain Upgrade and Retrieval Audit]].
- 2026-07-20 — Public marketing navigation keeps the accepted floating white header and original Minimalist mascot, while phone and tablet widths through 900px use an organized anchored sheet instead of a flat link stack. The sheet separates Discover (Home, Features, Pricing), Resources (Download, Story, FAQ), policy links, and account actions; its yellow app CTA remains visible while the destination region scrolls on short screens. The disclosure keeps the trigger as the keyboard entry point, closes on outside pointer/focus or Escape, restores trigger focus on Escape, and clears stale open state when crossing to the 901px desktop layout. Canonical sources: `src/pages/MarketingPages.jsx`, `public/base.css`, `tools/ui-smoke-test.mjs`, and `tools/audit-regression-check.mjs`. Verification passed targeted ESLint, the production build and performance budgets, 262 regression checks, 39 SEO build checks, the full desktop/mobile UI smoke suite, and direct browser checks at 320×568, 390×844, 768×1024, 844×390, and the 900/901 breakpoint. No deployment was performed.
- 2026-07-18 — Contacts avatar loading stays bounded but prioritizes what the user can see: the first six filtered contact rows use eager, high-priority image loading while remaining rows stay lazy. Hover, keyboard focus, and pointer/touch intent prewarm the exact selected avatar and the lazy public-profile bundle; opening the profile passes the already-known contact name/photo into the cache-first render, gives the popup image high fetch priority, and avoids resetting an unchanged image source before Firebase refresh completes. This directly mitigates legacy oversized avatars (live verification found a 6,120-pixel image displayed at 84 pixels); new uploads already pass through the 512×512 avatar optimization path, while old files remain large until their owners replace them. Canonical sources: `src/features/contacts/ContactsList.jsx`, `src/features/contacts/contactsService.js`, `src/features/profile/profilePopupService.js`, `src/pages/ChatPage.jsx`, and `tools/audit-regression-check.mjs`. Verification passed 261/261 regression checks, targeted ESLint, production build/performance budgets, and signed-in desktop plus 390×844 browser checks. No deployment was performed.
- 2026-07-18 — Private messaging keeps a bounded 80-message realtime tail and loads older Firebase push-key pages on demand, merging and deduplicating them without losing the reader's scroll anchor. Every dock open rehydrates the bounded PM inbox for the authenticated account; reopening the same person preserves the session while explicitly leaving stale mobile Inbox mode, and read PM-only contacts remain discoverable under Recent Messages. PM and Contacts participate in the shared floating-surface arbiter so Settings, Updates, profiles, and other utility UI cannot remain layered underneath; a user close restores Contacts only for a Contacts-originated PM, while navigation closes suppress that return. The PM single-pane contract matches the app breakpoint through 768px, with full-height vertical conversation history and split-pane layout beginning at 769px. Canonical sources: `src/features/private-messages/PrivateMessages.jsx`, `src/features/private-messages/pmHistoryModel.js`, `src/features/contacts/ContactsList.jsx`, `src/features/contacts/contactsService.js`, `src/features/shell/chatShellControls.js`, `public/features.css`, `tools/private-messages-model.test.mjs`, and `tools/private-messages-ui-regression.test.mjs`. This records the local implementation; no deployment was performed.
- 2026-07-17 — Room Catch-up remains default-on but is independently user-controllable from Settings → Appearance → Conversation tools. Store visibility per Firebase account on the current device; disabling Catch-up hides only its strip and actions, keeps Quick Replies available, and preserves the separate collapsed/expanded preference for re-enabling. Successful sends and dynamic footer-height changes may re-anchor the message rail only when the reader was already pinned to latest; never pull someone away from older history or interfere with pending history-anchor restoration. Canonical sources: `src/features/chat-core/ChatCore.jsx`, `src/features/chat-core/catchUpPreference.js`, `src/features/settings/settingsService.js`, `src/pages/ChatPage.jsx`, and `tools/room-catchup-preference.test.mjs`. This records the local implementation; no deployment was performed.
- 2026-07-17 — Keep Winston's protected local inference path bounded, measurable, and isolated. Server-owned room context must query bounded indexed candidate windows (120 messages, 64 tasks, 32 documents, 48 events), rank deterministic query-relevant evidence while retaining the newest items, include bounded document snippets, fence room data from prompt injection, frame harmless user preferences separately, and reserve input/output/safety tokens within the fixed 8,192-token window. The protected bridge remains bearer-authenticated and loopback-only on `127.0.0.1:8790`, forwards only the exact Fast `qwen3:4b-instruct`, Smart `qwen3:14b`, and Vision `qwen2.5vl:7b` allowlist to the isolated `127.0.0.1:11435` Ollama runtime, and must never inherit the tray model store or expose prompt/response bodies in logs. Schedule local inference through a strict FIFO weighted semaphore with four execution units (Fast/Smart/Vision weights 1/2/4), a bounded 100-request bridge queue, `OLLAMA_NUM_PARALLEL=4`, and `OLLAMA_MAX_QUEUE=100`; retain the separate durable 10/40/40 local/Cloudflare/Groq admission contract so provider overflow waits rather than disappearing. Authenticated telemetry may retain queue wait, duration, token counts/rates, model, route, and result only. Keep Flash Attention and `q8_0` KV-cache quantization opt-in until an A/B benchmark demonstrates a need; the RX 7900 XTX baseline completed 94 genuine Fast/Smart requests across 1–10 concurrent submissions with zero errors/timeouts, and the live upgraded bridge drained a ten-request mixed queue 10/10 back to zero. The tray Ollama endpoint is fixed to `127.0.0.1:11434`. Canonical sources: `functions/ai-context.js`, `functions/index.js`, `database.rules.json`, `tools/ollama-bridge/ollama-bridge.cjs`, `tools/ollama-bridge/managed-ollama-config.cjs`, `tools/ollama-bridge/BridgeControl.ps1`, `tools/ai-context.test.mjs`, and `tools/ai-local-benchmark.mjs`. The protected runtime was restarted locally; no Firebase/database-rules deployment was performed.
- 2026-07-17 — App audio stays selective and lightweight: use one gesture-unlocked, reusable Web Audio service for acknowledged room/PM sends, meaningful fresh alerts, direct-call ringing, genuine send failures, and newly completed quests or levels. Keep navigation, tabs, panel open/close, hover, typing, scrolling, routine toggles, reactions, loading, and reconnects silent. The per-device **App sounds** switch lives with Notifications and includes an explicit preview; Do Not Disturb silences every cue, quiet hours silence non-urgent cues while direct calls may remain audible, and WebRTC speech/OS notification volume stay separate. Foreground PMs use the in-app cue while background delivery belongs to browser/push alerts, active PMs count as visible only while the document is visible and focused, and cooldown/dedupe plus bounded node cleanup prevent overlapping or long-lived playback. Canonical sources: `src/features/audio/uiSoundService.js`, `src/features/notifications/notificationService.js`, `src/features/private-messages/pmInboxService.js`, `src/features/private-messages/PrivateMessages.jsx`, `src/features/chat-core/ChatCore.jsx`, `src/features/community/gamify.js`, and `tools/ui-sound-service.test.mjs`. No audio assets or external SFX dependency were added, and no deployment was performed.
- 2026-07-16 — The Pro personal-agent product identity is fixed as **Winston**, represented by the original friendly gorilla artwork at `public/assets/winston-gorilla-v1.webp`. Legacy or customized local/server profile names are normalized to Winston while instructions, tone, and memory remain personalizable; the name is no longer editable. Keep the standard sparkle AI glyph in desktop and mobile navigation; use Winston's gorilla portrait as his profile picture in the entitlement-locked introduction, unlocked identity header, and reply bubbles. The Pro lock screen must stay distinct from runtime states such as standby, warming, offline, authentication-blocked, queued, and ready; it uses truthful room-access/account-protection language and must not claim that a server-synced profile stays only on the device. Opening the drawer moves keyboard focus inside and closing restores the correct desktop/mobile trigger; the mobile surface remains a non-modal utility drawer because the persistent app navigation stays available. Upgrade routing closes Winston, waits for lazy Settings initialization, then selects and focuses Billing. Keep the 430px desktop drawer and full-width mobile surface scroll-safe through short 320×568 screens with the upgrade CTA, disclosure, and setup controls reachable, without changing authenticated `aiGateway`, Bananas, provider-routing, or server-owned Fast/Smart model identities. Visual source: `artifacts/design/winston-personal-agent-concept-v1.png`. Canonical sources: `src/features/ai/AI.jsx`, `src/features/ai/personalAgent.css`, `src/features/ai/localAiClient.js`, `src/features/shell/roomFeatureLoaders.js`, `src/pages/ChatPage.jsx`, `functions/index.js`, and `tools/winston-personal-agent.test.mjs`. This records the local implementation; no deployment was performed.
- 2026-07-15 — Keep site-wide performance enforceable in code: build budgets cap the largest JavaScript chunk, public/login route JavaScript, signed-in chat JavaScript/CSS, and entry HTML; active live chat has a 600-message hard ceiling, inactive scopes retain at most 240 messages across an eight-scope LRU, and sidebar rooms stream exact summary fields rather than full metadata trees. Reopening the same page or utility must coalesce same-context work, suppress stale async completion, reuse a host-aware React root, and keep all caches explicitly bounded. Room changes rescope only the default visible feature; data-heavy tabs may remain warm for a short revisit grace period before releasing listeners, while presence, editor, AI-request, and call lifecycles remain immediate where correctness requires it. Hidden views must stop listeners, timers, polling, handlers, and active requests; shared account services remain bounded and idempotent; valid network responses must never depend on a successful service-worker cache write; and still-image uploads should be prepared for their display role before Storage upload. Canonical sources: `tools/check-performance-budgets.mjs`, `src/features/chat-core/ChatCore.jsx`, `src/features/shell/featureMountCoordinator.js`, `src/features/shell/hostAwareRoot.js`, `src/features/shell/roomTabActivity.js`, `src/features/shell/roomFeatureLoaders.js`, `src/features/private-messages/PrivateMessages.jsx`, `src/features/ai/AI.jsx`, `src/lib/imageUploadOptimization.js`, and `public/sw.js`. Verification snapshot: [[30 Audits/Site-wide Performance Optimization 2026-07-15|Site-wide Performance Optimization 2026-07-15]].
- 2026-07-10 — Use additive Markdown imports; never mutate canonical source documents during vault ingestion.
- 2026-07-10 — Exclude generated report renderings and logs; summarize Lighthouse JSON into one history note.
- 2026-07-10 — Mark legacy product and legal copy as archived so it cannot be mistaken for current truth.
- 2026-07-10 — The original GBrain safeguard required explicit opt-in before embeddings or provider use. That opt-in was granted on 2026-07-21 for local Ollama embeddings only; hosted providers and background chat/judge workflows remain unapproved.
- 2026-07-11 — Keep message hover actions to React, Reply, and More on fine-pointer desktops; use a compact borderless translucent no-reflow toolbar beside the bubble, keep 40px controls in-flow on touch, and route Edit/Delete through the permission-checked overflow menu.
- 2026-07-11 — On phones, keep Catch-up to one compact row with icon actions and let the empty composer settle at 32px before auto-growing for multiline text.
- 2026-07-11 — Keep the landing-page demo visually faithful to the real Minimalist desktop shell but powered only by deterministic local state; never import ChatCore/Firebase into the public demo, and keep marketing motion to reduced-motion-safe opacity/transform transitions.
- 2026-07-12 — Keep private calls voice-first until lifecycle and connectivity prerequisites for video are complete. Direct-call history and alerts use validated `direct_call` metadata verified against a live `pm_calls` row; call reservation/status changes remain transactional and expiry-bounded; minimized calls retain reachable mute/hang-up controls; and PM sessions, decrypted caches, and pending media are cleared on Auth account changes. Canonical sources: `src/features/private-messages/PrivateMessages.jsx`, `src/features/private-messages/useDirectAudioCall.js`, `functions/index.js`, and `database.rules.json`.
- 2026-07-13 — Keep operational site analysis out of the public website and in the native, self-contained Windows app. AI availability exposes guarded Off, On, and Auto modes; Auto is the preferred mode and sleeps after the selected idle timeout (two hours by default). The app may control only the protected local bridge and approved Fast, Smart, and Vision model set, read administrator analytics, display a protected directory limited to normalized user labels plus exact Auth UIDs and membership/presence state, and run exact-ID moderation operations with typed plus No-default dialog confirmation. Exact Auth UID remains the action key; names and handles are display/search values only. Console commands must stay on a fixed live-schema allowlist, pass the central typed parser before confirmation, preflight exact Firebase targets before mutation, fail closed for invalid or protected content, and never become arbitrary shell/Firebase path input. App shutdown must cancel and await any Auth export so its raw temporary JSON is deleted before exit. Its Apple-style shell must remain responsive from a `900 × 640` compact window through wide desktop displays, using DPI-normalized Compact/Standard/Wide reflow and page-local vertical scrolling rather than horizontal overflow. See [[30 Audits/Minimalist Analysis Desktop App Review 2026-07-13|Minimalist Analysis Desktop App Review]].
- 2026-07-13 — Treat `tools/ai-analysis-app/design/PORCELAIN-SPEC.md` as the visual source of truth for Minimalist Analysis. Preserve the slim brand/freshness header, page-owned titles, unified KPI slab, white elevated surfaces on the cool Porcelain canvas, compact floating five-tab dock, flat system-blue charts, visible focus, High Contrast behavior, and responsive no-horizontal-overflow contract. Publishing must pass `verify-release.ps1` before a standalone package is handed off.
- 2026-07-13 — Website text AI exposes two server-owned profiles: Fast maps to `qwen3:4b-instruct`, while Smart maps to `qwen3:14b`. Clients persist and send only the `fast` or `smart` profile identifier; blank legacy requests default to Fast and unknown non-empty identifiers fail closed. Firebase owns the exact model mapping, and neither profile silently substitutes for the other. Both profiles use an 8,192-token context with thinking disabled. Vision remains a separate `qwen2.5vl:7b` path for calendar and photo extraction. The protected bridge and Minimalist Analysis app allowlist, health checks, and installers cover exactly these three tags. The bridge owns an isolated Ollama runtime on `127.0.0.1:11435` with the default user model store and must never control the user's tray Ollama on `11434` or inherit its alternate model store.
- 2026-07-13 — Keep one-off Google Calendar export explicit and per user: every Minimalist room event surface offers an “Add to Google” template link with encoded title, schedule, timezone, details, and location. This path does not depend on shared OAuth state or write a shared Google event ID. The existing manager-only OAuth connection remains the separate advanced import and automatic owner-sync path. Canonical sources: `src/features/calendar/googleCalendarLink.js`, `src/features/calendar/GoogleCalendarLink.jsx`, `src/features/calendar/Calendar.jsx`, `src/features/events/Events.jsx`, and `src/features/room-home/RoomHome.jsx`.
- 2026-07-18 — “Add this week to Google” is a visible-week, per-user Calendar action for all signed-in room members. It explicitly requests Google Calendar permission when no active token is available, keeps that one-off token in memory only, creates personal copies without writing shared RTDB `gId` state, and deduplicates repeat runs with private Google event properties while recognizing legacy `gId` only when it exists in the current user’s primary calendar. Google events created by this action are excluded from Calendar re-import so room events do not appear twice. The manager-only connection/import and automatic owner-sync path remains separate. Canonical sources: `src/features/calendar/Calendar.jsx`, `src/features/calendar/googleCalendarWeek.js`, `src/features/calendar/calendar.css`, and `tools/calendar-week-google-export.test.mjs`.
- 2026-07-19 — Calendar’s manager-only **Scan schedule** action exposes two explicit image sources: **Take photo** uses its own `accept="image/*" capture="environment"` input as a rear-camera hint, while **Import picture** uses a separate gallery/file input with no capture hint. Both inputs stay mounted, consume and clear the selected file so the same image can be retried, and feed the unchanged validation, client compression, AI extraction, duplicate filtering, room save, and optional Google-sync pipeline. The source chooser is an accessible disclosure that closes on Escape or outside press, remains in normal document flow at tablet/phone widths, stacks below 380px, and preserves readable mobile import progress; unsupported browsers may fall back to a normal picker because HTML capture is a hint rather than a camera guarantee. Canonical sources: `src/features/calendar/Calendar.jsx`, `src/features/calendar/calendarPhotoSelection.js`, `src/features/calendar/calendar.css`, and `tools/calendar-photo-sources.test.mjs`. This records the local implementation; no deployment was performed.
- 2026-07-13 — Treat Contacts as one lifecycle-managed drawer. Opening the already-open panel is idempotent; closing it invalidates queued renders, cancels mutual-room discovery, and releases Firebase/presence listeners; hidden panels must not schedule renders. Reuse the boot PM inbox stream, coalesce overlapping renders and per-profile reads, and route every Contacts-to-PM or utility-panel transition through `closeContactsPanel` rather than removing the CSS class directly. Canonical sources: `src/features/contacts/contactsService.js`, `src/features/contacts/ContactsList.jsx`, `src/features/private-messages/pmInboxService.js`, `src/features/profile/profilePopupService.js`, and `src/features/shell/roomFeatureLoaders.js`.
- 2026-07-13 — Public profile dialogs open from the safe Contacts/profile cache before network refreshes, expose an always-reachable 44px mobile close control, and guard every asynchronous enhancement with the active profile request so stale responses cannot repaint another person. A PM opened from Contacts carries a one-time `returnTo: 'contacts'` target; user close/Escape consumes it and reopens Contacts, while shell/navigation closes explicitly suppress it so other panels never rebound into Contacts. Canonical sources: `src/features/profile/profilePopupService.js`, `src/pages/ChatPage.jsx`, `src/features/contacts/contactsService.js`, `src/features/private-messages/PrivateMessages.jsx`, and `src/features/shell/chatShellControls.js`.
- 2026-07-14 — Production website AI uses the fixed named Cloudflare Tunnel hostname `https://ai.minimalist.chat`, routed only to the protected bridge at `http://127.0.0.1:8790`; never publish Ollama ports `11434` or `11435`, and never substitute a production Quick Tunnel. Tunnel intent is the strict, fail-closed `.bridge-control/public-tunnel.json` state. The exact `Cloudflared` service remains Manual, while the highest-privilege **Minimalist Chat Public Gateway Recovery** task reconciles intent at logon and once per minute, starts the bridge before publishing, and preserves intentional Off across restart. The Cloudflare connector token and `OLLAMA_SERVER_TOKEN` remain separate secrets; routine reconnects and connector-token rotations retain the hostname and require no Firebase redeploy. Canonical source: [[40 Operations/Protected Ollama Public Gateway|Protected Ollama Public Gateway]].
- 2026-07-14 — Remote Analysis uses a separate metadata-only agent on `127.0.0.1:8791` behind `analysis.minimalist.chat` and a one-owner Cloudflare Access application. The Windows client uses Managed OAuth as a public native client with dynamic registration, Authorization Code plus PKCE S256, a `127.0.0.1` callback, short memory-only access tokens, and a DPAPI-protected refresh session; remote administration, SSH, router forwarding, and fallback to local controls remain out of scope. The protected **Minimalist Chat Remote Analysis Agent** task starts only after the Windows owner signs in, runs directly as a hidden limited `WinExe` from protected LocalAppData, and requires the PC to remain awake and online. Canonical source: [[40 Operations/Remote Analysis Access|Remote Analysis Access]].
- 2026-07-15 — The repository's optional website text-AI router assigns active slots 1–10 to the protected local PC through Ollama, slots 11–50 to Cloudflare Workers AI with `@cf/qwen/qwen3-30b-a3b-fp8`, and slots 51–90 to Groq with `openai/gpt-oss-20b`. Request 91 and later overflow requests enter a durable FIFO queue and remain there until processed, explicitly cancelled by their owner, or permanently failed; capacity alone does not expire accepted work. Submission returns HTTP 202 rather than holding the request connection open, and the client follows completion through an owner-only realtime status/result mirror with authenticated polling recovery. Admission is bounded at 10,000 unfinished routed requests globally and 1,000 per owner, both above the 500-job stress target; accepted work is never evicted. A random ABA-safe reservation stays held across direct execution, queueing, running, and retries, and terminal job/admission markers plus a one-minute sweeper make release crash-recoverable. Banana receipts are atomic `charged`/`refunded` tombstones keyed to the durable job, so quota rollover cannot double-charge or replay a refunded request. Transient failures back off and exclude the failed provider without bypassing the FIFO head; incomplete provider configuration fails before charge and terminally refunds previously queued work. Terminal records may be retained for up to 24 hours. The no-cost two-client RTDB contention harness accepts 500 unique jobs plus 50 duplicate submissions, recovers interrupted claims, drains every job, and finishes with zero reservations and zero external AI calls; it is not a 500-socket load test. These hosted IDs are overflow implementations, not selectable client profile IDs or Ollama tags. Minimalist Analysis may display the 10/40/40 routing metadata in Local and Remote views, but hosted models remain read-only and must never enter `ApprovedOllamaModelProfile`, local install controls, or the protected bridge allowlist. Compact Remote Analysis keeps connection/session controls on a second header row so the `900 × 640` contract survives DPI and fallback-font changes without overlap. This records the repository contract, not a claim that it has been deployed. Canonical sources: `functions/ai-provider-routing.js`, `functions/ai-request-queue.js`, `functions/ai-queue-capacity.js`, `functions/index.js`, `database.rules.json`, `src/features/ai/localAiClient.js`, `tools/ai-queue-rtdb-contention-test.mjs`, `tools/ai-analysis-app/Logic/AnalysisAppLogic.cs`, and `tools/ai-analysis-app/ModernAnalysisForm.cs`.
- 2026-07-15 — Genuine staged AI load validation completed against the protected local Ollama gateway, Cloudflare Workers AI, and Groq while Firebase Auth and RTDB remained isolated in local emulators. The requested 100, 200, 400, and 1,000 stages completed 1,700/1,700 requests with zero failed or unresolved work: 303 direct admissions, 1,397 queued admissions, and final completions split 1,437 local / 155 Cloudflare / 108 Groq. Every stage reached the configured 90-lease high-water mark at exactly 10 / 40 / 40; the 1,000 stage peaked at 936 queued requests and ended with zero active reservations, pending jobs, provider leases, or wake slots. Groq free-tier TPM throttling produced retryable 429s, but the queue retained and rerouted the work without loss. Exact provider token accounting is not retained by the gateway; reply-length estimates total 11,478 output tokens for the four passing reports. The initial 100-stage report predates the strengthened wake-drain gate and recorded 12 redundant wake markers after jobs, reservations, and provider leases were already zero; the 200, 400, and 1,000 reports all passed the stricter full-zero drain check. Genuine probes exposed two Node 22 queue defects that were corrected locally: Firebase Admin server timestamps now use the modular `ServerValue` export, and queued completion uses the cold-cache-safe conditional transaction helper. The post-test AI routing, capacity, queue, gateway security, and protected bridge suite passes 55/55 tests. No production Firebase data was touched and no deployment was performed. Reports: `artifacts/ai-live-load/live-ai-mrmm5s85cee47d.json`, `live-ai-mrmm8cn76ea07e.json`, `live-ai-mrmmc666cf43e8.json`, and `live-ai-mrmmps76eff47e.json`. Canonical harness sources: `tools/run-ai-live-load-test.ps1`, `tools/ai-live-load-orchestrator.mjs`, `tools/ai-live-load-test.mjs`, `tools/ai-live-gateway-bridge.cjs`, and `tools/ai-live-queue-worker-runner.cjs`.
- 2026-07-14 — Account upgrades use hosted Stripe Checkout and the live Advanced/Pro catalog; the browser never owns Stripe secret state or embeds Checkout. Before creating account or room Checkout, Functions must retrieve the stored customer under the current Stripe mode, reuse it only when it belongs to the Firebase UID, and idempotently replace missing, deleted, wrong-owner, or test-mode records while clearing stale account entitlements. Existing live account subscriptions route to a dedicated account Customer Portal instead of creating duplicates; account and room portal configurations remain separate so plan changes cannot cross billing scopes. Canonical sources: `functions/index.js`, `functions/stripe-customer.js`, `src/features/billing/billingActions.js`, `src/features/billing/AccountBillingPanel.jsx`, and `tools/account-billing.test.mjs`.
